apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: petclinic-mysql-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: efs-zcp
